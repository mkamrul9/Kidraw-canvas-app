// ─── Color Palette ───────────────────────────────────────────
// Default color swatches shown in the PropertiesPanel
export const DEFAULT_COLORS = [
    '#0f172a', '#64748b', '#e11d48', '#d97706',
    '#059669', '#4f46e5', '#7c3aed', '#ffffff',
] as const;

// Maximum number of user-picked custom colors to remember
export const MAX_CUSTOM_COLORS = 5;

// Maximum visible board cards before "Show more" toggle
export const DASHBOARD_INITIAL_BOARD_COUNT = 4;
